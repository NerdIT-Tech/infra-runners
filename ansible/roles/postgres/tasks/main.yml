---
- name: Install PostgreSQL
  apt:
    name: "{{ postgresql_packages }}"
    state: present
    update_cache: true
  become: true

- name: Ensure PostgreSQL is running
  service:
    name: postgresql
    state: started
    enabled: true
  become: true

- name: Get PostgreSQL version
  command: psql --version
  register: pg_version_output
  changed_when: false

- name: Set pg_version variable
  set_fact:
    pg_version: "{{ pg_version_output.stdout | regex_search('\\d+') }}"

- name: Configure PostgreSQL to listen on all interfaces
  lineinfile:
    path: "/etc/postgresql/{{ pg_version }}/main/postgresql.conf"
    regexp: '^#?listen_addresses\s*='
    line: "listen_addresses = '*'"
  become: true
  notify: restart postgresql

- name: Allow remote connections in pg_hba.conf
  lineinfile:
    path: "/etc/postgresql/{{ pg_version }}/main/pg_hba.conf"
    regexp: '^host\s+{{ postgresql_db_name | regex_escape }}\s+{{ postgresql_db_user | regex_escape }}'
    line: "host    {{ postgresql_db_name }}    {{ postgresql_db_user }}    0.0.0.0/0    scram-sha-256"
  become: true
  notify: restart postgresql

- name: Allow PostgreSQL traffic in UFW
  ufw:
    rule: allow
    port: '{{ postgresql_port | default(5432) }}'
    proto: tcp
  become: true
  when: ufw_enabled | default(true)

- name: Force PostgreSQL restart to apply all changes
  service:
    name: postgresql
    state: restarted
  become: true

- name: Create {{ postgresql_db_user }} database user
  postgresql_user:
    name: "{{ postgresql_db_user }}"
    password: "{{ postgresql_db_pass }}"
    role_attr_flags: "CREATEDB"
  become: true
  become_user: postgres

- name: Create {{ postgresql_db_name }} database
  postgresql_db:
    name: "{{ postgresql_db_name }}"
    owner: "{{ postgresql_db_user }}"
  become: true
  become_user: postgres

- name: Grant connect privilege to {{ postgresql_db_user }} user
  postgresql_privs:
    db: "{{ postgresql_db_name }}"
    role: "{{ postgresql_db_user }}"
    privs: CONNECT
    type: database
  become: true
  become_user: postgres

- name: Create database schema
  postgresql_schema:
    db: "{{ postgresql_db_name }}"
    name: "{{ postgresql_db_schema }}"
    owner: "{{ postgresql_db_user }}"
  become: true
  become_user: postgres
